# ===========================================
# 🔒 SECRETS & CONFIGURATION
# ===========================================
# Copy this file to ".env" and fill in your actual values.
# The .env file is git-ignored to keep secrets out of version control.

# Admin password for Jenkins user "admin"
ADMIN_PASSWORD=changeme

# Full Jenkins URL (used for webhooks, emails, etc.)
# Include the trailing slash
JENKINS_URL=https://jenkins.example.com/

# Your domain name for Caddy's automatic HTTPS
# Do NOT include https:// or trailing slash
DOMAIN_NAME=jenkins.example.com
